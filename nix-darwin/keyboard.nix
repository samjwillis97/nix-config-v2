{
  system.keyboard = {
    enableKeyMapping = true;
    remapCapsLockToEscape = true;
    userKeyMapping = [
      # TODO: Caps Lock -> Escape
      # Globe -> Ctrl
      # Ctrl -> Globe
    ];
  };
}
